# Incident-Response-Prototype

1) download all files and directories of this repository
2) install Node.js with npm (https://nodejs.org/en/download/)
3) use command line to switch to the directory, in which you safed the files from 1)
4) type "npm run dev"
5) open the local host which will be listed
